version: '2'
services:
  #db:
  #  image: mongo

  ergatis:
    build: .
    ports:
      - "8080:80"
    volumes:
      - ./input_data:/mnt/input_data:ro

  #swarm:
  #  image: swarm

  #web:
  #  image: apache
  #  expose: 80
  #  links: ergatis

  #workflow:
  #  image: adkinsrs/workflow
